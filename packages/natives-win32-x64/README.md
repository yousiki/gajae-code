# @gajae-code/natives-win32-x64

Retired Windows x64 optional native addon package for `@gajae-code/natives`.

This fork no longer publishes Windows x64 (`win32-x64`) native addon packages; use Apple Silicon macOS or Linux x64 builds.
