Execute docs/gui-tui-parity-masterplan.md (synthesized 2026-07-07 from 4 architect audits) to close all validated GUI⇄TUI parity gaps in this worktree.

Global constraints (apply to every story):
- Every new backend-affecting method MUST be native Rust, strict-field, schema-registered `gjc/*` in crates/gjc-app-server (server.rs dispatch + schema.rs registration), with generated client wrapper in packages/gjc-app-server-client and unknown-field rejection tests. BANNED: gjc/commands/execute, TUI-handler replay, raw request strings.
- No raw secrets in GUI state/logs/exports. Provider auth = env-var references + redacted OAuth state machine only (docs/app-server-provider-auth.md).
- DESIGN.md is the visual SSOT (warm-dark, monospace, 1px hairlines, 2-4px radii, red-claw small-signal, no shadows). Terminal themes never take over app chrome.
- app-server owns session/runtime/config state; crates/gjc-desktop is OS integration only.
- Host seam changes go in packages/coding-agent/src/modes/app-server/agent-session-host.ts; GUI changes in packages/gjc-gui/src.
- Release gates per story: cargo test -p gjc-app-server; cargo clippy; bun run check:schemas; client check+test; GUI check+build+test.
- Update docs/gui-tui-parity-matrix.md rows honestly as part of each story (implemented/deferred status + evidence).

@goal: Session lifecycle truth — real resume, fork, delete, navigate, search
Close the P1 session blockers and P2 session depth gaps from masterplan §1.1, §1.2 and §2. (1) New strict `gjc/session/open { sessionPath }` routed through SessionManager.open(path) with the rename/export path policy, hydrating the transcript; add a Resume button per session-browser row (fixes hollow resumeThread==createThread at agent-session-host.ts:987-994). (2) Real fork: route factory.fork through AgentSession.branch/forkContextSeed seeded from the source thread + leaf entry; return forkedFromId in sessionMetadata (fixes forkThread==createThread at agent-session-host.ts:996-998). (3) New `gjc/session/delete { sessionPath }` calling deleteSessionWithArtifacts with a confirm dialog; document archive as ephemeral in the matrix. (4) New `gjc/session/navigate { threadId, entryId, summarize? }` → AgentSession.navigateTree and `gjc/session/label { threadId, entryId, label }` → appendLabelChange; make GUI tree nodes interactive (replace the <pre> dump). (5) Search parity: extend host search haystack to cwd/path/allMessagesText and add fuzzy scoring (port tui fuzzyFilter); add `cwd?` param to GjcSessionList/SearchParams so the GUI gets a this-folder/all scope toggle (replacing process.cwd() misuse at agent-session-host.ts:693-697). Acceptance: Rust strict/catalog/lane conformance tests for each new method; host tests (open hydrates history, fork preserves ancestry, delete removes file+artifacts, navigate/label round-trip, search matches by cwd/path/content); client wrapper drift tests; GUI tests for resume/fork/delete/tree interactions; all release gates green.

@goal: Live execution streaming — push parity for tool output, todos, jobs, lifecycle events
Close masterplan §1.3, §1.4 and the live-update P2 gaps. (1) Fold `tool_execution_update` in GUI transcript.ts foldRawEvent so tool cards stream partial output and subagent progress (event already arrives losslessly as raw gjc/event; optionally add a typed mapping in event_map.rs). (2) Subscribe GUI to todo_reminder/todo_auto_clear raw events to keep the todos card live. (3) New server notification `gjc/jobs/changed { threadId, kind: job|monitor|agent, id, status }` emitted from the async-job snapshot owner so jobs/agents/monitors cards update between turns; schema-register the notification and add it to the generated ServerNotificationEnvelope. (4) Rework exec-state refresh: debounce on activeTurnId turn boundaries + push events instead of transcript.items.length; never clear populated cards while refreshing (fixes the 7-RPC-per-item flicker at main.tsx:234-271). (5) Render lifecycle events currently dropped: auto_retry_start/end (retry countdown), ttsr_triggered, notice, auto_compaction_end outcome as inline transcript status blocks. (6) Workflow-gate resync: call gjcWorkflowGateList on connect/reconnect so pending gates survive reconnect. (7) Read-only monitors depth: map cron snapshots into jobs/monitors read models and add a bounded outputTail field to the monitors DTO. Acceptance: Rust notification schema + envelope tests; host emission tests; GUI folding tests for update/todo/lifecycle events; reconnect gate-resync test; no-flicker refresh test; release gates green.

@goal: Command catalog and palette truth — full catalog, dispatcher, display surfaces
Close masterplan §1.5, §1.6 and palette-related P2 gaps. (1) Host enumerates the FULL command registry in gjc/commands/list — builtin + file-based + skill + extension commands (fixes omission at agent-session-host.ts:834-861) — and sets CommandDescriptor.classification; delete the stale local classification map in command-palette-logic.ts:7-43 that disables 11 implemented commands. (2) Palette becomes a dispatcher: rows carry an action discriminator — navigate (open Model panel / Appearance / session browser), invoke (compact, retry, new, copy, dump), insert-prompt (prompt-display-only) — never send /model as chat text; add onClick activation + scrollIntoView on arrow nav. (3) Local HelpSheet and HotkeysSheet display surfaces so /help and /hotkeys never leak into the model prompt. (4) /drop composite: delete-current-then-start-new matching TUI builtin-registry.ts:979-985. (5) Extension inspect: store the gjc/extensions/inspect result and render a detail panel mirroring plugin inspect (fixes discarded response at main.tsx:375-382); extend ExtensionDescriptor with state/disabledReason/shadowedBy/provider grouping (read-only TUI dashboard depth). (6) Re-document /copy //dump matrix rows honestly (GUI-local serialization) and call gjcAuthStatus post-logout/reconnect or drop it from the row. Acceptance: host catalog test proves skill/extension/file commands present with classification; GUI palette dispatcher tests (navigate/invoke/insert paths); help/hotkeys no-backend-mutation tests; extension inspect panel test; release gates green.

@goal: Stale-deferral closures — move, provider add, OAuth login, model assign, toggles
Reclassify and implement the deferred rows whose blockers no longer exist (masterplan §3 + §2). (1) `gjc/session/move { threadId, targetCwd, dryRun? }`: dryRun returns { sourceSessionFile, targetSessionFile, artifactsDirs, crossDevice, conflicts[] }; non-dry requires not-streaming then flush()+SessionManager.moveTo() (rollback already exists at session-manager.ts:2926-3013); GUI refreshes the thread cwd label. (2) `gjc/provider/add` accepting env-var references only (preset | compat+providerId+baseUrl+apiKeyEnv+models[], force?) with reject-raw-key validation mirroring builtin-registry.ts:800-812; GUI add-provider form that never renders a key input. (3) OAuth login state machine per docs/app-server-provider-auth.md: gjc/auth/login/start {providerId} → {flowId, authUrl, state}; poll {flowId} → redacted {state, promptMessage?}; complete {flowId, redirectUrl}; cancel {flowId}; host flow-registry over AuthStorage.login with onManualCodeInput as the poll/complete slot; onPrompt-secret providers return state:"unsupported"; redaction tests mandatory (no tokens/verifiers/fingerprints in any DTO, log, or audit line); GUI LoginFlowSheet with browser-open via gjc-desktop. (4) `gjc/model/assign { role, provider, modelId, thinkingLevel? }` for per-role assignment parity with TUI /model <target> <model>; populate fast-mode affectedRoles from real role targets. (5) Toggle lane split from marketplace: `gjc/extensions/setEnabled { extensionId, enabled }` + skills equivalent writing only the disabledExtensions settings key through the Settings owner (catalog-validated, confirm-gated, no code execution); `gjc/plugins/setEnabled|setFeature|setSetting` as PluginManager runtime-config writes with secret-masked round-trip rejection and prev-value undo; GUI SkillExtensionToggles. Marketplace install/uninstall and /contribute-pr remain deferred. (6) Pin connection-level settings/auth/appearance methods to deterministic owners (process-global Settings.instance / connection AuthStorage or explicit threadId), fixing first-thread Map-order nondeterminism at agent-session-host.ts:1000-1020. Acceptance: strict/catalog/lane conformance + unknown-field tests per new method; redaction regression for the login flow; move dry-run + rollback tests; toggle round-trip tests proving no secret leak and undo; matrix rows reclassified with evidence; release gates green.

@goal: Beautiful embedding — layout blueprint, DESIGN.md conformance, keyboard-first UX
Implement masterplan §5 in packages/gjc-gui. (1) Three-column continuous frame: unified sidebar session list (merge Threads+Sessions into one list where loaded state is a status rail/label, tree branches as indentation, all actions per row), transcript center (920px max) with approvals interleaved at the blocked tool call instead of appended at bottom (main.tsx:975-991), collapsible 260px right rail hosting goal chip/card, todos, context/usage, jobs/agents/monitors, compaction — replacing the closed sidebar drawer. (2) Model panel split: catalog/thinking/fast → header model-chip popover; settings/provider-auth → palette-launched settings sheet; ExtensibilityPanel → overlay/right-rail panel instead of full workspace swap; split workspace-switcher into real tabs vs action buttons. (3) Keyboard-first: layered global Escape (overlay → queued steer → interrupt running turn via stopTurn()); Cmd/Ctrl+K palette; Cmd/Ctrl+N new thread; Enter-while-busy queues via turn/steer; roving tabindex on lists; theme preview on activate (not hover/focus); composer hint "Enter to send · Shift+Enter for newline". (4) Mechanical DESIGN.md fixes: style .exec-card (or reuse .exec-state-card); fix undefined --gjc-accent-red→--gjc-red-claw 2px rail and --gjc-font-mono→--gjc-font-ui in session-browser.css; square .status-badge (999px→4px); define --gjc-success-border/text; replace .jump-to-latest box-shadow with border; hit targets ≥32px; reconcile --gjc-text-dim with DESIGN.md; remove hardcoded #2a211c; drop dead void restoreAppearancePreview. (5) Replace window.prompt/window.confirm with the ConfirmDialog grammar (PromptRenameDialog). (6) Markdown: pipe-table rendering (monospace-aligned block minimum) + nested lists. (7) Richer theme preview: extend semanticPreview to the full semantic token set and render a sample transcript/tool-card block during preview (GUI-local; chrome untouched). (8) Export UX: native save dialog via gjc-desktop; raw export behind explicit confirm. Acceptance: GUI unit tests for layout/palette/escape/queueing/markdown-table/dialog behavior; visual QA screenshots per surface against DESIGN.md; no shadows/gradients/oversized radii introduced; release gates green.

@goal: Matrix reconciliation and final parity audit
Bring docs/gui-tui-parity-matrix.md to a truthful post-closure state and verify the whole run. (1) Add all 13 MISSING-FROM-MATRIX rows from masterplan §4 with correct classification and evidence (session open, navigate/label, persistent delete, model assign, provider add, HTML export decision, cwd scoping, live-update contract rows, cron/output-tail/ack-latch, extension metadata depth, gate resync, extension slash surfaces /notify //grok-build-usage //autoresearch //skill:*/file commands, /session delete verb, /agents quarantine correction). (2) Reclassify the stale deferrals implemented in this run to in-scope-new with evidence; keep /btw, goal mutation, /contribute-pr, marketplace honestly deferred with corrected unblock conditions. (3) Update the counted tally and per-goal row allocations. (4) Decide and record the HTML export disposition (implement in gjc/session/export format enum OR explicit exclusion row). (5) Run the full release-gate suite (cargo test -p gjc-app-server, cargo clippy, cargo test -p gjc-desktop, bun run check:schemas, client check+test, GUI check+build+test) and record results in the matrix audit section as a new dated closeout. (6) Final architect verification pass over the updated matrix against the shipped code — zero OVERCLAIMED rows remaining is the exit criterion. Acceptance: matrix internally consistent (tally matches rows), all gates green, architect verification report attached as evidence.
