**[ITERATE]**

**Justification**: The plan is directionally aligned with the spec and has the right big-picture decisions: Rust protocol/types as source of truth, Rust-owned thread registry, AgentSession-shaped backend seam, side-by-side migration before atomic deletion, and named dependent migrations. However, it is not approval-ready until the Architect must-fixes are folded into the executable plan as binding gates. The current Planner text still leaves executor guesswork around TS process-global isolation, N-API ordering/backpressure, immutable codex thread identity versus mutable GJC session identity, event normalization invariants, exact Codex v1 subset and command shape, schema-generation proof, and hard deletion gates at public API boundaries.

Verified source evidence:
- Spec `.gjc/_session-019f1c30-3754-7000-b634-75d54f2dc70c/specs/deep-interview-gjc-app-server.md` requires hard replacement of RPC plus notifications SDK, multi-thread `threadId -> AgentSession`, codex core plus gjc extensions, stdio/WS/UDS transports, generated TS plus JSON Schema, and seven acceptance criteria.
- Planner `.gjc/_session-019f1c51-6f3a-7000-89c2-5e2fcbb61ee2/plans/ralplan/019f1c51-6f3a-7000-89c2-5e2fcbb61ee2/stage-01-planner.md` covers the scope and sequences build/migrate/delete, but its Phase 0/1/2 gates do not yet make the hardest concurrency and identity constraints test-blocking.
- Architect `.gjc/_session-019f1c68-2b8e-7000-b198-2579da71ac07/plans/ralplan/019f1c68-2b8e-7000-b198-2579da71ac07/stage-02-architect.md` identifies must-fixes that are real blockers, not polish.
- `packages/coding-agent/src/sdk.ts` sets process-wide search/image preferences and, for top-level sessions, `setActiveSkills`, `setActiveRules`, `AsyncJobManager.setInstance`, and `MCPManager.setInstance`. This directly affects the app-server multi-thread-in-one-process design.
- `packages/coding-agent/src/modes/rpc/rpc-mode.ts` has an explicit ordered command scheduler with cancellation/read fast lanes. The app-server plan must preserve this causal ordering per thread rather than merely say bounded queues.
- `crates/pi-natives/src/notifications.rs` uses `ThreadsafeFunctionCallMode::NonBlocking` for a narrower single-session notifications callback path. It is insufficient proof for many concurrent AgentSession backends.
- `packages/coding-agent/src/session/agent-session.ts` emits `message_update` before queued extension handling and defers external `agent_end` until prompt in-flight state unwinds. Codex item lifecycle requires a state machine with terminal coalescing, not a simple mapping table.
- `packages/agent/src/types.ts` shows the agent event set is rich enough to create interleaving and terminal-ordering hazards.
- `packages/coding-agent/src/harness-control-plane/rpc-adapter.ts` currently treats acceptance as ack plus next `agent_start`; migration must preserve a testable equivalent for M10.
- `python/gjc-rpc/src/gjc_rpc/client.py` and `python/robogjc/src/worker.py` are live RPC dependents with blocking client/event semantics, host tools, resume, cancellation, and per-task timeouts.
- `scripts/generate-json-schemas.ts` currently emits only `schemas/config.schema.json` and `schemas/models.schema.json`; `package.json` wires `generate-schemas` and `check:schemas` to that script. The Rust protocol schema path must be proven early.
- Legacy public boundary references were verified in `packages/coding-agent/src/cli.ts`, `packages/coding-agent/src/cli/args.ts`, `packages/coding-agent/src/main.ts`, `packages/coding-agent/src/modes/index.ts`, `packages/coding-agent/src/modes/rpc/rpc-client.ts`, `packages/coding-agent/src/commands/harness.ts`, `docs/rpc.md`, `docs/external-control-readiness.md`, and `docs/bot-integration.md`.

Representative implementation simulations:
1. Creating `ThreadRegistry` plus TS `AgentBackendFactory`: an executor can allocate Rust `threadId`s and create AgentSession objects, but without a Phase 0 isolation contract they must guess whether process globals become app-server-wide singletons or per-session keyed state. Disposing one thread could clear managers needed by another. This blocks confident execution.
2. Implementing `event_map.rs`: the Planner mapping tables identify inputs/outputs, but current AgentSession timing means a mapper must maintain per-turn generation, item accumulators, content-index ordering, and terminal coalescing. Without that state-machine contract and fixtures, executors can produce duplicate `item/completed`, deltas before `item/started`, or premature idle.
3. Rewriting `GajaeCodeRpc` harness adapter: the plan names M10 e2e and says acceptance comes from `turn/start` response plus `turn/started` or `gjc/agent/start`, which is close, but the revised plan must specify the exact invariant replacing current ack plus agent_start, including cursor behavior and idle pre-state. Otherwise M10 can pass only accidentally or become flaky.
4. Running atomic deletion: the plan correctly deletes legacy code after migrations, but source inspection shows deletion must include CLI mode literals/dispatch, SDK/modes exports, harness env override names, Python dataclasses/client protocol, docs, native `NotificationServer` exports, and crate/package references. This must be a hard gate after all app-server suites pass, not a best-effort search.
5. Adding schema generation: current schema CI has no app-server protocol generator. Phase 0 must prove Rust-derived JSON Schema/TS generation and sample-frame validation, or later protocol work can drift under a green `check:schemas`.

**Summary**:
- Clarity: Good high-level scope and modules, but approval-blocking ambiguity remains in TS global isolation, generation-token identity, command-lane ordering, and Codex subset behavior.
- Verifiability: The plan names most suites, but per-phase acceptance must be tightened so each phase has explicit pass/fail gates tied to the spec seven ACs and named suites: app-server conformance, harness M10 e2e, python `gjc-rpc` tests, robogjc tests, notifications e2e, codex interop smoke, and `check:schemas`.
- Completeness: Scope coverage is broad and mostly correct. Missing binding detail: multi-thread isolation audit, TSFN failure semantics, immutable thread/generation model, exact `command/exec` argv/base64 behavior, and public-boundary deletion gate.
- Big Picture: Fits the spec and future Rust-core direction. Side-by-side build/migrate/delete is the right strategy. Do not switch to a TS-owned facade unless Rust ownership proves infeasible.
- Principle/Option Consistency: Consistent with Rust protocol source of truth, AgentSession-shaped backend seam, and no-backcompat deletion. The plan must avoid contradicting this by leaving TS globals or legacy RPC exports as hidden authorities.
- Alternatives Depth: Fair enough on protocol type source of truth, event mapping, and registry ownership. The TS-owned registry/facade alternative is acknowledged and reasonably rejected for this spec.
- Risk/Verification Rigor: Current risk table is useful but insufficient until the Architect concurrency/isolation risks become concrete mitigation tasks and tests. Bounded queues and non-droppable core events need exact queue, lane, overload, and TSFN-error rules.

Required fixes before APPROVE, prioritized:
1. Add a Phase 0 multi-thread isolation audit and binding execution contract for every process-wide singleton/global touched by AgentSession creation and disposal: `AsyncJobManager`, `MCPManager`, active skills/rules, internal URL handlers, provider/search/image preferences, eval/browser ownership, extension/runtime state, and notification/MCP callbacks. Decide app-server-wide owner versus per-thread keyed state. Add a two-thread test that runs async jobs/MCP/internal URL or equivalent isolated resources, disposes one thread, and proves the other continues.
2. Specify per-thread command scheduling and N-API callback flow control: serial mutating lane, cancellation/read fast lanes, bounded ingress/egress queues, overload rejection before accepting `turn/start`, TSFN call failure handling, lifecycle event non-droppability, and diagnostics/counters for any intentionally droppable gjc detail events.
3. Add a formal identity model: Rust-owned immutable codex `threadId`; TS `sessionId`/`sessionFile` only backend metadata; backend generation token on every callback; stale `emitBackendEvent(threadId, generation, event)` rejected; explicit semantics for `gjc/session/new`, switch, branch, resume, delete, unload, and reconnect.
4. Replace event mapping tables with a tested Rust state-machine deliverable: item accumulators, content-index mapping, per-turn generation, terminal coalescing, `item/started` before deltas, exactly one completion per item/turn, abort/failure semantics, raw `gjc/event` preservation, and fixtures for interleaved text/thinking/tool/toolcall/compaction/queued steer/follow-up/redacted thinking.
5. Pin the Codex v1 reference subset and method matrix: for every codex field/method mark accept, ignore/project, reject with JSON-RPC error, or gjc-extension only. Include `turn/steer.expectedTurnId`, approval fields as no-op, thread statuses, and `command/exec` argv plus streaming output/base64 or explicitly documented multiplexing. Add codex interop smoke and conformance fixtures.
6. Make schema generation a Phase 0 blocking gate: Rust protocol derives/generator command, generated TS and JSON Schema artifacts, integration with `bun run generate-schemas` and `bun run check:schemas`, and schema validation for sample Codex and gjc frames including JSON-RPC envelope/header expectations.
7. Tighten per-phase acceptance criteria so each phase is independently bounded and maps to named suites: Phase 0 conformance/schema proof; Phase 1 two-thread isolation plus basic JSONL turn; Phase 2 app-server conformance plus host tool/URI/gjc extension tests; Phase 3 transport/discovery tests; Phase 4 harness adapter unit plus M10 e2e; Phase 5 python `gjc-rpc` tests; Phase 6 robogjc tests plus `bun run test:py`; Phase 7 notifications daemon e2e for Telegram/Discord/Slack discovery/action/reconnect; Phase 8 codex interop smoke; Phase 9 full `bun run check`, `bun run test`, `bun run build:native`, `bun run test:py`, Rust workspace tests, and reference-search gate.
8. Add rollback-safe cutover language: build app-server side-by-side, migrate each dependent and prove its named suite, keep old RPC/notifications callable until all replacements pass, then delete legacy surfaces atomically. Deletion must explicitly cover CLI flags/mode literals, `runRpcMode` dispatch, RPC SDK exports/types/client, harness env overrides, Python protocol dataclasses/client compatibility paths, docs, native `NotificationServer` exports, `gjc-notifications` crate/package references, and obsolete tests.
9. Define notifications parity acceptance concretely: WS discovery/token behavior, idle/action_needed/context_update/turn_stream/activity events, inbound user message/config command, pending ask replay, first-valid-reply-wins, idempotent terminal resolution, reconnect, and per-daemon Telegram/Discord/Slack e2e.

Verdict: ITERATE. Execution should not be approved until the revised plan incorporates these as explicit deliverables and verification gates.
