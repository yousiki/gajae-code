# GUI ⇄ TUI Parity / API Contract Matrix (G001 stage-zero gate)

Status: G008-VERIFIED. Gate approved (G001); feature goals G003–G007 implemented + reviewed; final audit (G008) verified this matrix. See the "G008 Final Audit" section at the end.
Scope source: approved consensus plan stage-02-revision.md + user-confirmed intent reconciliation.

## Scope freeze (user-confirmed)
- "Full TUI parity" = operator-level parity for interactive chat/session surfaces. Terminal/process-control flows are EXCLUDED with rationale, not literally reproduced.
- Existing schema-typed app-server core methods (`thread/*`, `turn/*`) and existing `gjc/*` methods are APPROVED for GUI backend-affecting use as-is: codex-core methods stay lenient per `field_policy.rs`; `gjc/*` methods stay strict. Every NEW backend-affecting method added by this run MUST be native Rust, strict-field, schema-registered `gjc/*`. BANNED for backend-affecting behavior: generic `gjc/commands/execute`, `command/exec`, `thread/shellCommand`, TUI-handler replay, GUI raw request strings, and `gjc/notifications/*` (reserved for the opaque notifications SDK exception only). Session lifecycle rows (`thread/loaded/list`, `thread/resume`, `thread/read`, `thread/fork`, `thread/delete`, `thread/archive`) are therefore `in-scope-existing`, not contradictions.
- Theme parity = DESIGN-compatible previews/semantic tokens; terminal themes never take over app chrome. Root DESIGN.md remains the visual SSOT.
- Native macOS hooks (dialogs, open-url/OAuth handoff, keychain, window/icon) are OS-integration ONLY; app-server owns all session/runtime/config state.

## Classification legend and decision rule
- `in-scope-existing` — an existing app-server method/notification already supports it; implement GUI + generated wrapper now.
- `in-scope-new` — a NEW native strict `gjc/*` method that is FULLY SPECIFIED in this doc (method name, params, strict field policy, schema/DTO owner); implement now.
- `deferred-needs-new-api` — desirable but the API/policy is not yet designed; state the unblock condition.
- `prompt-display-only` — produces a prompt/display payload only; no session/runtime mutation.
- `excluded-terminal-only` — terminal/process-control; no desktop equivalent.

Decision rule: a row is in-scope ONLY if it is `in-scope-existing` OR `in-scope-new` with a fully specified method; otherwise it is deferred.

## Generated-client SSOT repair targets (feeds G002)
The generator (`packages/gjc-app-server-client/scripts/generate.ts`) emits protocol TYPES ONLY. `AppServerRequestMap`, wrapper methods, and the `ServerNotificationEnvelope` union are hand-maintained and already LAG the server. G002 must generate or mechanically drift-check them from a Rust-owned catalog. Confirmed gaps:
- Existing server methods with NO client wrapper/request-map entry: `thread/loaded/list`, `thread/fork`, `thread/delete`, `thread/archive`, `gjc/hostUriSchemes/set`, `gjc/hostUris/result`, `gjc/workflowGate/list`, `gjc/workflowGate/respond`, `gjc/unattended/negotiate`, `gjc/unattended/audit` (dispatch `server.rs:787-817`).
- Published-but-untyped notifications (absent from `ServerNotificationEnvelope` in generated `protocol.ts:450-458`): `gjc/hostUris/request` (`server.rs:409-420`), `gjc/hostUris/cancel` (`server.rs:537-568`), `gjc/workflowGate/opened` (`server.rs:457-462`). Schemas already registered (`schema.rs:100-119`).
- Already-wrapped baseline: initialize, `thread/start`, `thread/resume`, `thread/read`, `turn/start`, `turn/steer`, `turn/interrupt`, `gjc/state/read`, `gjc/messages/get`, `gjc/model/set`, `gjc/todos/set`, `gjc/compact`, `gjc/hostTools/*`.

## Security requirements (provider/auth/credentials)
- No raw API key / token / secret may appear in GUI state, logs, error text, screenshots, or exported diagnostics.
- Provider auth uses native browser/keychain handoff; app-server owns state transitions + redaction. Any row that cannot prove a token-safe boundary from a designed app-server API is `deferred-needs-new-api`.
- `/provider add` accepts env-var references only and rejects raw `--api-key` (parity with TUI boundary at `builtin-registry.ts:800-873`).

## Evidence ledger: command palette / slash commands (G003)

| Surface | Class | Method(s) & notification | DTO/schema owner | Client wrapper status | Tests required (backend/client/gui) | Visual evidence | G008 status | Rationale / unblock |
|---|---|---|---|---|---|---|---|---|
| catalog discovery (builtin + file + skill + extension) | in-scope-new | `gjc/commands/list`; params `{ includeDisabled?: boolean }`; result `{ commands: CommandDescriptor[] }`; no notification | Rust `commands` DTO; strict `gjc/*`; schema registered in `schema.rs` | generated request-map + wrapper | backend schema/strict reject unknown fields; client wrapper drift; GUI palette renders catalog/classification | `artifacts/parity/qa/g003-quality-gate.json`; red-team; GUI screenshots | implemented | Enumerates builtin commands, file-based custom commands, `/skill:*`, and extension commands with populated classification; metadata only, no command execution path. |
| extension slash surfaces (`/notify`, `/grok-build-usage`, `/autoresearch`, `/skill:*`, file custom commands) | in-scope-new | `gjc/commands/list` | `CommandDescriptor` classification/provider/source fields | generated wrapper | command catalog strict/schema tests; palette dispatcher tests; GUI screenshots | `artifacts/parity/qa/g003-quality-gate.json`; red-team; GUI screenshots | implemented | These slash surfaces are represented as catalog entries and dispatched by the GUI palette without `gjc/commands/execute` or TUI-handler replay. |
| `/settings` | in-scope-new | `gjc/settings/schema`, `gjc/settings/read`, `gjc/settings/update` | safe allowlist Rust settings DTOs | generated wrappers | schema strict/catalog/lane; client drift; GUI settings controls; persistence flush | gui build/tests | implemented | Token-safe UI/behavior allowlist only; credentials/paths excluded. Token-regex hits in this row are token-count/budget counters, not secrets. |
| `/theme` | in-scope-new | `gjc/appearance/themes/list`, `gjc/appearance/read`, `gjc/appearance/set`; preview/restore GUI-local only | strict Rust appearance DTOs; semantic preview tokens from real theme registry | generated wrappers | schema strict/catalog/lane; client drift; GUI preview state; host registry/settings | gui build/tests | implemented | DESIGN.md remains visual SSOT: terminal themes set terminal settings only and never take over app chrome; no OSC probing. |
| `/goal` | in-scope-new (read) + deferred-needs-new-api (mutation) | `gjc/goal/read` | `GjcGoalReadParams` / `GjcGoalReadResult` | generated `gjcGoalRead` wrapper | strict schema/catalog/read-lane; host maps `AgentSession.getGoalModeState()` | GUI goal status chip/card | read implemented; mutation deferred-needs-new-api | Read exposes active/objective/status/tokensUsed only from real goal-mode state. Goal MUTATION is deferred-needs-new-api: unblock requires a strict goal-mutation DTO plus an approved AgentBackend goal-mutation seam owning create/update/complete/drop/pause/resume with provenance + confirmation policy; no goal-mutation method exists in `server.rs`/`schema.rs` today (live probes return -32601). |
| `/model` | in-scope-new | `gjc/model/set`, `gjc/model/catalog`, `gjc/model/assign` | strict Rust model DTOs | generated wrappers | backend schema/catalog/lane; client wrapper; GUI catalog/dropdown/role assignment | `artifacts/parity/qa/g004-quality-gate.json`; red-team; GUI screenshots | implemented | Catalog exposes provider/model metadata only; per-role assignment writes through `gjc/model/assign` with no keys/base URLs. Token-regex hits are token-count capacity metadata, not secrets. |
| `/fast` | in-scope-new | `gjc/fast/read`, `gjc/fast/set` | strict Rust fast DTOs | generated wrappers | backend schema/catalog/lane; client wrapper; GUI toggle | gui build/tests | implemented | Uses real AgentSession fast-mode owner. Token-regex hits are token-count counters elsewhere in the schema/catalog, not secrets. |
| `/export` | in-scope-new | `gjc/session/export` | `GjcSessionExportParams` / `GjcSessionExportResult` | generated `gjcSessionExport` wrapper | strict schema/catalog/lane; host path-policy + non-empty markdown/json export + redaction/raw regression; client wrapper test | `cargo test -p gjc-app-server`; `bun run check:schemas`; `bun test packages/coding-agent/test/app-server-session-export.test.ts`; client check+test; `artifacts/parity/qa/g011-fix-verification.json` | implemented | Local desktop-only absolute `.jsonl`; host rejects traversal/nonexistent files; redaction defaults on; server provenance includes exportedAt/sessionId/sourcePath/redacted/tool. |
| `/copy` | in-scope-existing | `gjc/messages/get`; GUI clipboard write; no notification | existing messages DTO | wrapped baseline | backend messages smoke; client wrapper; GUI clipboard mock | copy confirmation screenshot | implemented | Clipboard serialization is GUI-side from already fetched messages. |
| `/dump` | in-scope-existing | `gjc/messages/get`; GUI-side text serialization; no new server method | existing messages DTO | wrapped baseline | backend messages smoke; client wrapper; GUI serialized text snapshot | dump text preview screenshot | implemented | Definitive v1 scope: dump current messages already obtained via `gjc/messages/get`; server-side redacted dump is separate deferred session row. |
| `/session` | in-scope-new | `thread/loaded/list`, `thread/resume`, `thread/read`, `gjc/session/open`, `gjc/session/list`, `gjc/session/search`; `cwd?` on list/search | existing thread DTOs + strict `GjcSessionOpen*`/`SessionIndexEntry`/`GjcSessionListResult`/`GjcSessionSearchResult` | generated client wrappers: loaded-list/read/resume + `gjcSessionOpen`/`gjcSessionList`/`gjcSessionSearch` | backend conformance strict/catalog/lane; TS host fake sessions; fuzzy deep-content search; client wrapper tests | `artifacts/parity/qa/g001-quality-gate.json`; red-team; GUI screenshots | implemented | Persistent local desktop session index/search/open now uses strict native `gjc/*`; path fields are local-only desktop paths; `cwd?` scopes this-folder vs all history. |
| `/jobs` | in-scope-new | `gjc/jobs/list`; notification `gjc/jobs/changed`; params `{ threadId }`; result includes jobs plus cron/read-model fields where present | Rust `GjcJobsList*`, `GjcCronEntry`, `GjcJobsChanged*` DTOs; strict `gjc/*`; schema registered | generated `gjcJobsList` wrapper + typed notification/raw event map | backend strict/catalog/read-lane conformance; notification envelope/event_map; TS host fake job-snapshot mapping; client wrapper | `artifacts/parity/qa/g002-quality-gate.json`; red-team; GUI screenshots | implemented | Jobs snapshot from `AgentSession.getAsyncJobSnapshot()` plus push refresh via `gjc/jobs/changed`; cron rows are represented by `GjcCronEntry`. |
| `/context` | in-scope-new | `gjc/context/read` (see execution-state ledger row) | Rust `GjcContextRead*` DTOs | generated `gjcContextRead` wrapper | backend strict/read-lane; host mapping; client wrapper | GUI exec-state context card | implemented | Token-safe context usage read model. |
| `/usage` | in-scope-new | `gjc/usage/read`; params `{ threadId }`; result `{ perModel: [{ provider?, modelId, input, output, cacheRead?, cacheWrite?, cost? }], totalCost?, source, freshness }` | Rust `GjcUsageRead*` DTOs; strict `gjc/*`; schema registered | generated `gjcUsageRead` wrapper | backend strict/catalog/read-lane; host mapping; client wrapper | GUI exec-state usage card | implemented | Per-model token/cost aggregates from `getSessionStats()`; provider quota limits remain a residual (no quota API exists). |
| `/help` | prompt-display-only | optional `gjc/commands/list`; no mutation | command/help display DTO | catalog wrapper if used | GUI test proves no backend mutation path | help panel screenshot | implemented | Display-only help surface. |
| `/hotkeys` | prompt-display-only | none; local display payload | GUI static DTO | none | GUI test proves no backend mutation path | hotkeys panel screenshot | implemented | Display-only keyboard reference. |
| `/tools` | in-scope-new | `gjc/tools/list`; params `{}`; result `{ tools: ToolDescriptor[] }`; no notification | Rust tools DTO; strict `gjc/*`; schema registered | generate wrapper | backend schema/strict; client wrapper; GUI list cards | tools panel screenshot | implemented | Read-only tool metadata. |
| `/agents` | in-scope-new | `gjc/agents/list`; params `{ threadId }`; result `{ agents: [{ id, agentType?, description?, status, outputRef? }] }` | Rust `GjcAgentsList*` DTOs; strict `gjc/*`; schema registered | generated `gjcAgentsList` wrapper | backend strict/catalog/read-lane; host mapping against real `metadata.subagent` job shape; client wrapper | `artifacts/parity/qa/g002-quality-gate.json`; red-team; GUI screenshots | implemented | GUI agents dashboard is subagent-typed async jobs (`metadata.subagent`). TUI `/agents` itself is quarantined, so this row is not claiming a separate live-agent inventory command. |
| `/monitors` | in-scope-new | `gjc/monitors/list`; params `{ threadId }`; result includes monitors, `crons: GjcCronEntry[]`, and bounded `outputTail` where present | Rust `GjcMonitorsList*`, `GjcCronEntry` DTOs; strict `gjc/*`; schema registered | generated `gjcMonitorsList` wrapper | backend strict/catalog/read-lane; host mapping against real `metadata.monitor === true` job shape; client wrapper | `artifacts/parity/qa/g002-quality-gate.json`; red-team; GUI screenshots | implemented | Monitors are monitor-flagged background jobs; read model includes cron snapshots and bounded output tails. Monitor cancel/cron delete remains a future mutating contract, not claimed here. |
| `/tree` | in-scope-new | `gjc/session/tree`, `gjc/session/navigate`, `gjc/session/label` | `GjcSessionTreeResult` / `SessionTreeNodeDto` plus strict navigate/label DTOs | generated wrappers | backend conformance strict/catalog/lane; TS host active-path/label/preview/navigate tests; client wrapper test | `artifacts/parity/qa/g001-quality-gate.json`; red-team; GUI screenshots | implemented | Live-thread branch tree read model preserves ancestry, labels, active path, and <=120-char previews; navigation and labels are strict native mutations. |
| `/provider` | in-scope-new | `gjc/provider/list`, `gjc/provider/add` | `GjcProviderList*`, `GjcProviderAdd*` | generated wrappers | strict schema/catalog/read+mutating lanes; env-var-only validation; host fake auth storage; client wrapper; GUI provider panel | `artifacts/parity/qa/g004-quality-gate.json`; red-team; GUI screenshots | implemented | Metadata-only provider list plus token-safe env-var onboarding. `gjc/provider/add` rejects raw API keys and accepts env-var references only. |
| `/login` | in-scope-new | `gjc/auth/login/start`, `gjc/auth/login/poll`, `gjc/auth/login/complete`, `gjc/auth/login/cancel` | redacted OAuth login-flow DTOs from `docs/app-server-provider-auth.md` | generated wrappers | strict schema/catalog/lane; redacted state-machine tests; client wrapper; GUI login sheet | `artifacts/parity/qa/g004-quality-gate.json`; red-team; GUI screenshots | implemented | OAuth is now a resumable redacted state machine; unsupported prompt-secret providers report redacted unsupported state rather than exposing credentials. |
| `/logout` | in-scope-new | `gjc/auth/logout`; `gjc/auth/status` | `GjcAuthLogout*`, `GjcAuthStatus*` | generated `gjcAuthLogout` / `gjcAuthStatus` wrappers | strict schema/catalog/lane; host fake auth storage; client wrapper; GUI sign-out confirmation | GUI provider panel | implemented | Logout calls existing `AuthStorage.remove(providerId)` and writes one redacted audit line with provider id only. |
| `/ssh` | excluded-terminal-only | none | none | none | excluded | excluded | excluded | Raw SSH terminal is excluded; SSH host-config management is a separate deferred capability, not v1. |
| `/new` | in-scope-existing | `thread/start`; existing turn/event notifications | existing thread DTOs | wrapped baseline | backend thread/start; client wrapper; GUI new-session flow | new session screenshot | implemented | Existing core thread lifecycle method. |
| `/drop` | in-scope-existing | `thread/delete` for loaded session | existing thread DTOs | wrapper needed | backend delete; client wrapper; GUI delete confirmation | delete screenshot | implemented | Existing core lifecycle method; loaded-session scope only. |
| `/compact` | in-scope-existing | `gjc/compact` | existing compact DTO | wrapped baseline | backend compact; client wrapper; GUI compaction action | compact screenshot | implemented | Existing strict `gjc/*` method. |
| `/contribute-pr` | deferred-needs-new-api | none approved | TBD artifact/SCM DTO | none | deferred | deferred | deferred | Needs SCM credential policy, artifact provenance, and dry-run/review gates. |
| `/resume` | in-scope-existing | `thread/resume` | existing thread DTO | wrapped baseline | backend resume; client wrapper; GUI resume flow | resume screenshot | implemented | Existing core lifecycle method. |
| `/btw` | deferred-needs-new-api | none approved | ephemeral side-turn DTO TBD | none | deferred | deferred | deferred | Needs an ephemeral side-turn API mirroring `runEphemeralTurn` (no tools, no persistence, current-session context); `thread/start` creates persistent independent threads and is not equivalent. |
| `/retry` | in-scope-new | `gjc/retry` | `GjcRetryParams` / `GjcRetryResult` | generated `gjcRetry` wrapper | strict schema/catalog/mutating-lane; host calls `AgentSession.retry()` | Retry last turn action | implemented | Real owner is `AgentSession.retry()`, also used by TUI `/retry`; returns turnId shape and no generic TUI replay. |
| `/background` | excluded-terminal-only | none | none | none | excluded | excluded | excluded | Terminal/process-control flow with no desktop equivalent. |
| `/debug` | excluded-terminal-only | none | none | none | excluded | excluded | excluded | Terminal/debug process-control flow with no desktop equivalent. |
| `/memory` | deferred-needs-new-api | none approved | TBD memory read/clear/enqueue DTOs | none | deferred | deferred | deferred | Store owner exists, but GUI parity still needs a redaction/provenance-safe read/clear/enqueue contract and policy before exposing memory state or mutation. |
| `/rename` | in-scope-new | `gjc/session/rename` | `GjcSessionRenameParams` / `GjcSessionRenameResult` | generated `gjcSessionRename` wrapper | strict schema/catalog/lane; host absolute path-policy + valid tmp rename; client wrapper test | `cargo test -p gjc-app-server`; host path-policy/rename test; client check+test; `artifacts/parity/qa/g011-fix-verification.json` | implemented | Offline connection-level route opens local desktop-only absolute `.jsonl` sessionPath and persists `setSessionName(..., "user")`; title 1..200 chars after trim. |
| `/move` | in-scope-new | `gjc/session/move` | `GjcSessionMoveParams` / dry-run + apply result DTOs | generated `gjcSessionMove` wrapper | strict schema/catalog/mutating-lane; dry-run/path-policy/not-streaming/rollback tests; client wrapper; GUI move flow | `artifacts/parity/qa/g004-quality-gate.json`; red-team; GUI screenshots | implemented | Moves use the real SessionManager move seam with dry-run, path policy, not-streaming guard, flush, and rollback-aware application. |
| `/exit` | excluded-terminal-only | none | none | none | excluded | excluded | excluded | Terminal process lifecycle control; desktop window close is OS chrome, not app-server parity. |

## Evidence ledger: sessions (G004)

| Surface | Class | Method(s) & notification | DTO/schema owner | Client wrapper status | Tests required (backend/client/gui) | Visual evidence | G008 status | Rationale / unblock |
|---|---|---|---|---|---|---|---|---|
| loaded threads list | in-scope-existing | `thread/loaded/list` (`server.rs:794-796`) | existing Rust thread DTO | wrapper needed | backend list; client wrapper; GUI list render | loaded sessions screenshot | implemented | Existing schema-typed core method approved. |
| resume selected | in-scope-existing | `thread/resume` (`server.rs:789`) | existing Rust thread DTO | wrapped baseline | backend resume; client wrapper; GUI resume | resumed session screenshot | implemented | Existing core method. |
| read thread state | in-scope-existing | `thread/read` (`server.rs:793`) | existing Rust thread DTO | wrapped baseline | backend read; client wrapper; GUI transcript hydrate | transcript screenshot | implemented | Existing core method. |
| fork | in-scope-existing | `thread/fork` (`server.rs:790`) | existing Rust thread DTO | wrapper needed | backend fork; client wrapper; GUI branch create | fork screenshot | implemented | Existing core method. |
| delete loaded session | in-scope-existing | `thread/delete` (`server.rs:791`) | existing Rust thread DTO | wrapper needed | backend delete; client wrapper; GUI delete | delete screenshot | implemented | Existing loaded-session lifecycle method. |
| archive loaded session | in-scope-existing | `thread/archive` (`server.rs:792`) | existing Rust thread DTO | wrapper needed | backend archive; client wrapper; GUI archive | archive screenshot | implemented | Existing loaded-session lifecycle method. |
| persistent list / history search | in-scope-new | `gjc/session/list`, `gjc/session/search`; optional `cwd?` | `SessionIndexEntry`, `GjcSessionListResult`, `GjcSessionSearchResult` | generated `gjcSessionList` / `gjcSessionSearch` wrappers | Rust strict unknown-field + lane/catalog/schema tests; TS host fake SessionManager list/search; fuzzy deep-content search; client wrapper tests | `artifacts/parity/qa/g001-quality-gate.json`; red-team; GUI screenshots | implemented | Maps `SessionManager.list(cwd)`/`listAll()`; fuzzy search covers id/title/cwd/path/deep message text; local absolute paths accepted for local-only desktop GUI. |
| session open / resume-from-history | in-scope-new | `gjc/session/open` | `GjcSessionOpenParams`, `GjcSessionOpenResult` | generated `gjcSessionOpen` wrapper | strict schema/catalog/lane; SessionManager.open hydration; client wrapper; GUI resume button | `artifacts/parity/qa/g001-quality-gate.json`; red-team; GUI screenshots | implemented | Opens a persisted `.jsonl` session into a real hydrated thread instead of creating an empty replacement session. |
| real fork / branch seed | in-scope-existing | `thread/fork` with `forkContextSeed` and `forkedFromId` metadata | existing thread DTO + session metadata | generated wrapper | backend fork ancestry tests; client wrapper; GUI branch create | `artifacts/parity/qa/g001-quality-gate.json`; red-team; GUI screenshots | implemented | Fork now preserves ancestry/context via real fork seed and returns `forkedFromId`; no hollow create-thread fallback. |
| session tree / branch nav | in-scope-new | `gjc/session/tree`, `gjc/session/navigate`, `gjc/session/label` | `GjcSessionTreeResult`, `SessionTreeNodeDto`, strict navigate/label DTOs | generated wrappers | Rust strict unknown-field + lane/catalog/schema tests; TS host tree active-path/label/preview/navigate test; GUI flatten/indent/interactions | `artifacts/parity/qa/g001-quality-gate.json`; red-team; GUI screenshots | implemented | Uses live thread tree owner; GUI renders interactive branch nodes, supports navigation, and persists labels. |
| rename | in-scope-new | `gjc/session/rename` | `GjcSessionRenameParams`, `GjcSessionRenameResult` | generated `gjcSessionRename` wrapper | strict schema/catalog/lane; host path-policy + valid tmp rename; client wrapper test | `cargo test -p gjc-app-server`; `bun test packages/coding-agent/test/app-server-session-export.test.ts`; client check+test; `artifacts/parity/qa/g011-fix-verification.json` | implemented | Connection-level offline rename via local desktop-only absolute `.jsonl` sessionPath; title 1..200 chars after trim. |
| move workspace | in-scope-new | `gjc/session/move` | `GjcSessionMoveParams`, `GjcSessionMoveResult` | generated `gjcSessionMove` wrapper | strict schema/catalog/mutating-lane; dry-run/path-policy/not-streaming/rollback tests; client wrapper; GUI move flow | `artifacts/parity/qa/g004-quality-gate.json`; red-team; GUI screenshots | implemented | Uses SessionManager move with dry-run, path policy, flush, and rollback-aware apply semantics. |
| persistent delete / `/session delete` verb | in-scope-new | `gjc/session/delete` | `GjcSessionDeleteParams`, `GjcSessionDeleteResult` | generated `gjcSessionDelete` wrapper | strict schema/catalog/mutating-lane; managed-root delete; client wrapper; GUI confirm | `artifacts/parity/qa/g001-quality-gate.json`; red-team; GUI screenshots | implemented | Deletes persisted sessions and managed artifacts through the approved managed-root policy; this records the missing `/session delete` verb. |
| server-side redacted export/dump | in-scope-new | `gjc/session/export` formats `markdown` and `json` | `GjcSessionExportParams`, `GjcSessionExportResult`, provenance DTO | generated `gjcSessionExport` wrapper | redaction regression for `sk-*`, `Bearer`, AWS access keys, GitHub/Slack tokens, and nested key-name secrets; raw `redact:false`; non-empty handcrafted session markdown/json; 5MB cap | `cargo test -p gjc-app-server`; `bun test packages/coding-agent/test/app-server-session-export.test.ts`; client check+test; `artifacts/parity/qa/g011-fix-verification.json` | implemented | Redaction defaults true; key-name values are replaced with `[REDACTED]`; raw export only when `redact:false`; nonexistent/relative session paths fail cleanly. |
| HTML session export | deferred-needs-new-api | not implemented; `gjc/session/export` supports `markdown` and `json` only | add `html` to `GjcSessionExportParams.format` enum and provenance DTO | none | deferred | deferred | deferred | TUI `exportToHtml` was not implemented in this run. Unblock by adding `html` to the strict `gjc/session/export` format enum with redaction/provenance tests and GUI save/export evidence. |

## Evidence ledger: model / thinking / provider / fast / settings (G005)

| Surface | Class | Method(s) & notification | DTO/schema owner | Client wrapper status | Tests required (backend/client/gui) | Visual evidence | G008 status | Rationale / unblock |
|---|---|---|---|---|---|---|---|---|
| set active model | in-scope-existing | `gjc/model/set` (`server.rs:804`) | existing Rust model DTO | wrapped baseline | backend set; client wrapper; GUI selector | model selector screenshot | implemented | Existing strict `gjc/*` method. |
| model catalog + assignments read/write | in-scope-new | `gjc/model/catalog`, `gjc/model/assign` | `GjcModelCatalog*`, `GjcModelAssign*` | generated wrappers | cargo app-server; schemas; client check/test; GUI build/tests | `artifacts/parity/qa/g004-quality-gate.json`; red-team; GUI screenshots | implemented | Token-safe provider/model metadata plus per-role assignment; no keys/base URLs. |
| thinking read/set | in-scope-new | `gjc/thinking/read`, `gjc/thinking/set` | `GjcThinking*` | generated wrappers | cargo app-server; schemas; client check/test; GUI build/tests | gui model panel | implemented | Session state owner; persisted wherever AgentSession/session manager persists thinking changes. Token-regex hits are token-count counters, not secrets. |
| fast status/toggle | in-scope-new | `gjc/fast/read`, `gjc/fast/set` | `GjcFast*` | generated wrappers | cargo app-server; schemas; client check/test; GUI build/tests | gui model panel | implemented | Uses AgentSession `isFastModeActive`/`setFastMode`; affectedRoles defaults to default. Token-regex hits are token-count counters, not secrets. |
| settings schema/read/update | in-scope-new | `gjc/settings/schema`, `gjc/settings/read`, `gjc/settings/update` | `GjcSettings*` | generated wrappers | cargo app-server; schemas; client check/test; GUI build/tests | gui model panel | implemented | Safe allowlist: theme.dark, theme.light, notification bell booleans, autoResume; updates flush through the real Settings owner. Token-regex hits are token-count counters, not secrets. |
| provider onboarding / OAuth / import credentials | in-scope-new | `gjc/provider/add`, `gjc/auth/login/start`, `gjc/auth/login/poll`, `gjc/auth/login/complete`, `gjc/auth/login/cancel` | `GjcProviderAdd*` plus redacted OAuth login-flow DTOs | generated wrappers | strict schema/catalog/lane; env-var-only provider add; redacted OAuth state-machine tests; client wrapper; GUI provider/auth sheets | `artifacts/parity/qa/g004-quality-gate.json`; red-team; GUI screenshots | implemented | Provider add is env-var only and rejects raw keys; OAuth login is a redacted resumable start/poll/complete/cancel state machine. |
| logout / credential revocation | in-scope-new | `gjc/auth/logout`; `gjc/auth/status` | `GjcAuthLogout*`, `GjcAuthStatus*` | generated wrappers | strict schema/catalog/lane; host fake auth storage; client wrapper; GUI confirmation | gui model panel | implemented | Existing auth-storage removal path only; audit is provider id only. |

## Evidence ledger: themes / skills / extensions / plugins (G006)

| Surface | Class | Method(s) & notification | DTO/schema owner | Client wrapper status | Tests required (backend/client/gui) | Visual evidence | G008 status | Rationale / unblock |
|---|---|---|---|---|---|---|---|---|
| themes/list | in-scope-new | `gjc/appearance/themes/list` | strict Rust `GjcAppearanceThemesList*`; semanticPreview only | generated wrapper | schema strict/catalog/lane; client drift; GUI populated/empty/CJK logic | gui tests | implemented | Maps real theme registry/loader to dark/light semantic swatches; no full terminal dump. |
| theme/read | in-scope-new | `gjc/appearance/read` | strict Rust `GjcAppearanceRead*` | generated wrapper | schema strict/catalog/lane; host Settings owner read | gui tests | implemented | Reads real Settings owner: `theme.dark`, `theme.light`, `symbolPreset`, `colorBlindMode`. |
| theme/set | in-scope-new | `gjc/appearance/set` | strict Rust `GjcAppearanceSet*` | generated wrapper | strict unknown-field + at-least-one; host validates ids and flushes settings | gui tests | implemented | Persists only terminal theme setting ids and appearance toggles through Settings owner. |
| theme/preview | in-scope-gui-local | none (GUI local state) | none | none | preview state machine test | gui tests | implemented-as-GUI-local | Headless app-server has no desktop/chrome preview runtime; GUI previews semantic swatches locally from last-read appearance. |
| theme/restorePreview | in-scope-gui-local | none (GUI local state) | none | none | restore-on-cancel/disconnect state test | gui tests | implemented-as-GUI-local | Restore means discard GUI candidate and return to last-read appearance; no server mutation needed. |
| symbolPreset/read+set | in-scope-new | `gjc/appearance/read`, `gjc/appearance/set` | included optional fields on strict appearance DTOs | generated wrapper | host Settings owner read/set + flush | gui tests | implemented | Real keys exist in `settings-schema.ts`: `symbolPreset` enum unicode/nerd/ascii. |
| colorBlindMode/read+set | in-scope-new | `gjc/appearance/read`, `gjc/appearance/set` | included optional fields on strict appearance DTOs | generated wrapper | host Settings owner read/set + flush | gui tests | implemented | Real key exists in `settings-schema.ts`: `colorBlindMode` boolean. |
| raw terminal background detection (OSC) | excluded-terminal-only | none | none | none | excluded | excluded | excluded | GUI chrome is CSS/DESIGN-token driven; terminal OSC probing is not portable desktop UX. |
| skills catalog/status/read | in-scope-new | `gjc/skills/list`; params `{ includeDisabled?: boolean }`; result `{ skills: SkillDescriptor[] }`; no notification | Rust skills DTO; strict `gjc/*`; schema registered | generate wrapper | backend schema/strict; client wrapper; GUI catalog | skills catalog screenshot | implemented | Read-only catalog/status only. |
| skills enable/settings | in-scope-new | `gjc/skills/setEnabled` | strict skills toggle DTO | generated `gjcSkillsSetEnabled` wrapper | strict schema/catalog/mutating-lane; settings-owner write; client wrapper; GUI confirmation | `artifacts/parity/qa/g004-quality-gate.json`; red-team; GUI screenshots | implemented | Toggle writes the catalog-validated settings key only; marketplace/install behavior remains out of this row. |
| extensions catalog/status | in-scope-new | `gjc/extensions/list`; params `{ includeDisabled?: boolean }`; result `{ extensions: ExtensionDescriptor[] }` including `state`, `disabledReason`, `shadowedBy`, `provider`; no notification | Rust extensions DTO; strict `gjc/*`; schema registered | generated wrapper | backend schema/strict; client wrapper; GUI catalog | `artifacts/parity/qa/g003-quality-gate.json`; red-team; GUI screenshots | implemented | Read-only catalog/status includes extension metadata depth matching TUI: shadowed state, disabled reason, and provider grouping. |
| extensions inspect | in-scope-new | `gjc/extensions/inspect`; params `{ extensionId: string }`; result `{ extension: ExtensionInspection }`; no notification | Rust extensions DTO; strict `gjc/*`; schema registered | generate wrapper | backend schema/strict; client wrapper; GUI detail panel | extension detail screenshot | implemented | Read-only inspect is allowed. |
| extensions enable/settings | in-scope-new | `gjc/extensions/setEnabled` | strict extension toggle DTO | generated `gjcExtensionsSetEnabled` wrapper | strict schema/catalog/mutating-lane; settings-owner write; client wrapper; GUI confirmation | `artifacts/parity/qa/g004-quality-gate.json`; red-team; GUI screenshots | implemented | Toggle writes the catalog-validated settings key only; install/marketplace remains deferred below. |
| installed plugin catalog/status (`gjc/plugins/list`) | in-scope-new | `gjc/plugins/list`; params `{ includeDisabled?: boolean }`; result `{ plugins: PluginDescriptor[] }`; no notification | Rust plugins DTO; strict `gjc/*`; schema registered; secret-masked fields only | generate wrapper | backend list strict + masking; client wrapper; GUI catalog/status list | plugin catalog screenshot | implemented | Read-only catalog/status list; no mutation. |
| installed plugin inspect/readSettings (`gjc/plugins/inspect`) | in-scope-new | `gjc/plugins/inspect`; params `{ pluginId: string, includeSettings?: boolean }`; result `{ plugin: PluginInspection }`; no notification | Rust plugins DTO; strict `gjc/*`; schema registered; secret-masked settings only | generate wrapper | backend inspect strict + masking; client wrapper; GUI detail/settings view | plugin detail screenshot | implemented | Read-only inspect + settings view; mutations deferred below. |
| plugin setEnabled/setFeature/setSetting | in-scope-new | `gjc/plugins/setEnabled`, `gjc/plugins/setFeature`, `gjc/plugins/setSetting` | strict plugin mutation DTOs; secret-masked settings | generated wrappers | strict schema/catalog/mutating-lane; masked round-trip rejection; previous-value undo; client wrapper; GUI controls | `artifacts/parity/qa/g004-quality-gate.json`; red-team; GUI screenshots | implemented | Runtime config-file mutations only; no marketplace install/uninstall or unreviewed code execution is claimed. |
| plugin marketplace install/uninstall | deferred-needs-new-api | none approved | TBD marketplace/provenance DTO | none | deferred | deferred | deferred | Backend/security/provenance-sensitive. Unblock condition: signature/source/trust verification, dry-run preview, rollback, secret-masking preserved through read/write cycles, explicit permission policy, artifact provenance metadata, a policy that forbids executing unreviewed marketplace code, and independent supply-chain security review before any GUI install/uninstall control is exposed. |
| raw extension terminal widgets / gen/debug | excluded-terminal-only | none | none | none | excluded | excluded | excluded | Raw terminal widgets and generator/debug terminal flows are not portable to desktop chrome. |

## Evidence ledger: execution-state (G007, + early hardening primitives)

| Surface | Class | Method(s) & notification | DTO/schema owner | Client wrapper status | Tests required (backend/client/gui) | Visual evidence | G008 status | Rationale / unblock |
|---|---|---|---|---|---|---|---|---|
| host-tool call notification | in-scope-existing | notification `gjc/hostTools/call` | existing host-tool DTO | typed in `ServerNotificationEnvelope` (`protocol.ts:457`) | backend emit; client notification; GUI card | tool call screenshot | implemented | Existing published typed notification. |
| host-tool result method | in-scope-existing | `gjc/hostTools/result` (`server.rs:808`) | existing host-tool DTO | wrapped baseline | backend result; client wrapper; GUI state update | result screenshot | implemented | Existing strict `gjc/*` method. |
| host-tool update method | in-scope-existing | `gjc/hostTools/update` (`server.rs:809`) | existing host-tool DTO | wrapped baseline | backend update; client wrapper; GUI partial update | update screenshot | implemented | Existing strict `gjc/*` method. |
| host-tool cancel notification | in-scope-existing | notification `gjc/hostTools/cancel` | existing host-tool DTO | typed in `ServerNotificationEnvelope` (`protocol.ts:458`) | backend emit; client notification; GUI cancel state | cancel screenshot | implemented | Existing published typed notification. |
| host URI schemes/set | in-scope-existing | `gjc/hostUriSchemes/set` (`server.rs:810`) | existing host URI DTO; schemas `schema.rs:112-119` | wrapper needed | backend strict; client wrapper; GUI registration status | schemes screenshot | implemented | Existing strict method; client gap only. |
| host URI result | in-scope-existing | `gjc/hostUris/result` (`server.rs:811`) | existing host URI DTO; schemas `schema.rs:112-119` | wrapper needed | backend strict; client wrapper; GUI response | host URI result screenshot | implemented | Existing strict method; client gap only. |
| host URI request notification | in-scope-existing | notification `gjc/hostUris/request` (`server.rs:409-420`) | existing host URI DTO; schemas `schema.rs:112-119` | notification union missing (`protocol.ts:450-458`) | backend emit; client notification type; GUI prompt | host URI request screenshot | implemented | Published notification; type in G002. |
| host URI cancel notification | in-scope-existing | notification `gjc/hostUris/cancel` (`server.rs:537-568`) | existing host URI DTO; schemas `schema.rs:112-119` | notification union missing (`protocol.ts:450-458`) | backend emit; client notification type; GUI dismiss | host URI cancel screenshot | implemented | Published notification; type in G002. |
| workflow gate list + reconnect resync | in-scope-existing | `gjc/workflowGate/list` (`server.rs:812`); client calls `gjcWorkflowGateList` on connect/reconnect | existing workflow gate DTO; schemas `schema.rs:100-103` | generated wrapper | backend list; client wrapper; GUI reconnect resync | `artifacts/parity/qa/g002-quality-gate.json`; red-team; GUI screenshots | implemented | Existing strict method; GUI now resyncs pending gates after reconnect instead of relying only on live opened notifications. |
| workflow gate respond | in-scope-existing | `gjc/workflowGate/respond` (`server.rs:813`) | existing workflow gate DTO; schemas `schema.rs:100-103` | wrapper needed | backend respond; client wrapper; GUI approve/reject | gate response screenshot | implemented | Existing strict method; client gap only. |
| workflow gate opened notification | in-scope-existing | notification `gjc/workflowGate/opened` (`server.rs:457-462`) | existing workflow gate DTO; schemas `schema.rs:100-103` | notification union missing (`protocol.ts:450-458`) | backend emit; client notification type; GUI prompt | gate opened screenshot | implemented | Published notification; type in G002. |
| tools cards (call/result/error/cancel) | in-scope-existing | existing event stream `gjc/event` + host-tool notifications | existing event/host-tool DTOs | baseline + notification gaps above | backend event map; client stream; GUI transcript folding | tool card screenshot | implemented | Use existing stream; harden display/folding. |
| `tool_execution_update` streaming fold | in-scope-new | raw `gjc/event` `tool_execution_update`; `event_map` maps jobs_changed | event-map/transcript event DTOs | generated event union + raw fallback | event-map tests; GUI transcript folding tests; browser screenshot | `artifacts/parity/qa/g002-quality-gate.json`; red-team; GUI screenshots | implemented | GUI folds partial tool output and nested progress instead of waiting for terminal result; no generic command execution path. |
| live jobs/monitors change notification | in-scope-new | notification `gjc/jobs/changed`; raw event map key `jobs_changed` | `GjcJobsChangedNotification` DTO; strict schema registered | typed notification envelope + raw mapping | backend notification envelope; client notification union; GUI refresh on push/turn boundary | `artifacts/parity/qa/g002-quality-gate.json`; red-team; GUI screenshots | implemented | Push contract refreshes jobs, agents, monitors, todos, and exec-state cards between turns without polling storms. |
| lifecycle status event folding | in-scope-new | raw `gjc/event` lifecycle events `auto_retry_*`, `ttsr_triggered`, `notice`, `auto_compaction_end`, `todo_reminder`, `todo_auto_clear` | event-map/transcript status DTOs | generated/raw event handling | GUI transcript/status tests; browser screenshot | `artifacts/parity/qa/g002-quality-gate.json`; red-team; GUI screenshots | implemented | GUI renders retry countdowns, TTSR notices, notice lines, compaction outcomes, and todo reminder/auto-clear events as inline status blocks. |
| compaction start | in-scope-existing | `gjc/compact` (`server.rs:806`) | existing compact DTO | wrapped baseline | backend compact; client wrapper; GUI action | compact screenshot | implemented | Existing strict `gjc/*` method. |
| compaction summary/read | in-scope-new | `gjc/compact/summary`; params `{ threadId }`; result `{ summaries: [{ id?, summary, tokensBefore?, timestamp }] }` | Rust `GjcCompactSummary*` DTOs; strict `gjc/*`; schema registered | generated `gjcCompactSummary` wrapper | backend strict/catalog/read-lane; host mapping with `scrubSecrets` + 2000-char truncation test; client wrapper | GUI compaction card + showcase | implemented | Summaries from session compaction entries; secret-scrubbed and truncated (retention = session file lifetime). |
| todos read/list/status | in-scope-new | `gjc/todos/read`; params `{ threadId }`; result `{ todos: [{ id?, content, status }] }` | Rust `GjcTodosRead*` DTOs; strict `gjc/*`; schema registered | generated `gjcTodosRead` wrapper | backend strict/catalog/read-lane; host mapping; client wrapper | GUI todos card + showcase | implemented | Read model complements existing `gjc/todos/set`; sourced from session todo state. |
| context usage | in-scope-new | `gjc/context/read`; params `{ threadId }`; result `{ tokens: { input, output, cacheRead?, cacheWrite?, total }, contextWindow?, percentUsed?, source, freshness }`; no notification | Rust `GjcContextRead*` DTOs; strict `gjc/*`; schema registered | generated types + `gjcContextRead` wrapper | backend strict unknown-field + read-lane + catalog/schema tests; client wrapper drift; TS host fake-session mapping | `cargo test -p gjc-app-server`; `bun test packages/coding-agent/test/app-server-agent-host.test.ts`; client check/test | implemented | Token accounting source is `AgentSession.getSessionStats()` plus `getContextUsage()`; token-safe numeric read model only, no provider secrets. |
| provider usage limits | in-scope-new | `gjc/usage/read` (per-model aggregates) | Rust `GjcUsageRead*` DTOs | generated `gjcUsageRead` wrapper | backend strict/catalog/read-lane; host mapping; client wrapper | GUI usage card | implemented | Ships per-model token/cost aggregates; PROVIDER QUOTA LIMITS remain a residual — no quota/limit source exists in the runtime; revisit when a provider-quota API is designed. |
| jobs snapshot | in-scope-new | `gjc/jobs/list`, notification `gjc/jobs/changed` | Rust `GjcJobsList*`, `GjcCronEntry`, `GjcJobsChanged*` DTOs | generated `gjcJobsList` wrapper + typed notification | backend strict/catalog/read-lane; host mapping (dedup by id); client wrapper/notification | `artifacts/parity/qa/g002-quality-gate.json`; red-team; GUI screenshots | implemented | Stable job identities + lifecycle statuses from `getAsyncJobSnapshot()`; includes cron read-model entries where present and push refresh on lifecycle changes. |
| agents dashboard | in-scope-new | `gjc/agents/list` | Rust `GjcAgentsList*` DTOs | generated `gjcAgentsList` wrapper | backend strict/catalog/read-lane; host mapping from `metadata.subagent`; client wrapper | GUI agents card | implemented | Agent inventory = subagent-typed async jobs; documented surface semantics. |
| monitors dashboard | in-scope-new | `gjc/monitors/list`, notification `gjc/jobs/changed` | Rust `GjcMonitorsList*`, `GjcCronEntry` DTOs | generated `gjcMonitorsList` wrapper | backend strict/catalog/read-lane; host mapping from `metadata.monitor === true`; client wrapper | `artifacts/parity/qa/g002-quality-gate.json`; red-team; GUI screenshots | implemented | Monitor ownership/lifecycle from monitor-flagged background jobs; read model includes `crons[]` and bounded `outputTail`. |
| retry last turn | in-scope-new | `gjc/retry` | `GjcRetryParams` / `GjcRetryResult` | generated `gjcRetry` wrapper | strict schema/catalog/mutating-lane; host fake retry test; client wrapper | GUI Retry last turn button | implemented | Uses real `AgentSession.retry()` seam; no TUI-controller replay. |
| failed-job ack / monitor cancel / cron delete mutations | deferred-needs-new-api | none approved | TBD job/monitor mutation DTOs | none | deferred | deferred | deferred | Read models shipped, but acknowledgement/cancel/delete are mutating controls; unblock with strict confirmation-gated DTOs owned by the async-job/monitor runtime. |
| terminal/process-control exec flows | excluded-terminal-only | none | none | none | excluded | excluded | excluded | Raw terminal/process-control is excluded from desktop parity. |

## Counted tally

| Class | Rows (G001 freeze) | Rows (current, parity burn-down run) |
|---|---:|---:|
| in-scope-existing | 28 | 27 |
| in-scope-new | 7 | 58 |
| deferred-needs-new-api | 50 | 6 |
| prompt-display-only | 2 | 2 |
| in-scope-gui-local | 0 | 2 |
| in-scope-new (read) + deferred-needs-new-api (mutation) | 0 | 1 |
| excluded-terminal-only | 7 | 7 |
| **Total** | **94** | **103** |

The current column reflects the GUI⇄TUI parity burn-down run across G001-G005 plus this G006 reconciliation. Added rows account for masterplan §4 items that were missing from the matrix: `gjc/session/open`, real fork metadata, `gjc/session/navigate`, `gjc/session/label`, `gjc/session/delete`, `gjc/model/assign`, `gjc/provider/add`, `cwd?` list/search scoping, live-update contracts, cron/outputTail read models, extension metadata depth, workflow-gate reconnect resync, extension slash surfaces, `/session delete`, and the explicit HTML-export disposition. Stale deferrals now closed are `/move`, `/login` OAuth, provider add, skills/extensions toggles, plugin settings mutations, and per-role model assignment. Honest residual deferrals remain `/btw`, goal mutation, `/memory`, `/contribute-pr`, plugin marketplace install/uninstall, failed-job/monitor/cron mutations, and HTML export.

| Goal | Row allocation |
|---|---:|
| G003 command palette / slash commands | 37 |
| G004 sessions | 15 |
| G005 model / thinking / provider / fast / settings | 7 |
| G006 themes / skills / extensions / plugins | 18 |
| G007 execution-state | 26 |
| **Total** | **103** |

## G008 signoff mechanics
- No `in-scope-existing` or `in-scope-new` row may be marked implemented unless the row links all required evidence: method/schema test, client request-map/wrapper or notification-union test, GUI behavior test, and visual evidence.
- `in-scope-new` evidence must additionally prove the new method is native Rust, strict-field, schema-registered `gjc/*`, with generated wrapper coverage and unknown-field rejection.
- `deferred-needs-new-api` and `excluded-terminal-only` rows must link rationale plus unblock condition; they must not be silently implemented through generic command execution, TUI handler replay, raw request strings, or shell/process-control paths.
- `prompt-display-only` rows must link evidence that no backend mutation path is used; any optional read-only catalog call must be separately covered by its catalog row.
- G008 is a verification/signoff pass only: it records implemented/deferred/excluded state against this matrix and does not create substitute scope.

## G008 Final Audit (verification, not creation)

This audit verifies the frozen G001 matrix against the shipped implementation. Per-row `pending` G008-status entries are resolved here by class disposition; each implemented class links to its per-goal quality-gate receipt + evidence artifacts.

### Per-class final disposition
- **in-scope-existing + in-scope-new (implemented + verified):** command catalog + tools list including `/theme` routing to Appearance (G003); session lifecycle loaded-list/resume/read/fork/delete/archive (G004); model catalog/set, thinking, fast, and settings schema/read/update (G005); appearance themes/read/set, symbolPreset, colorBlindMode, skills/extensions/plugins read catalogs incl. includeDisabled/includeSettings + masked plugin inspect (G006); host-URI + workflow-gate cards, compaction control, and all UI-hardening primitives (G007). Every backend-affecting method is native strict `gjc/*` (or an approved existing schema-typed core `thread/*`/`gjc/*`), schema-registered, generated-client-wrapped, with strict unknown-field tests + GUI tests + live visual QA. Appearance preview/restore remains GUI-local by design: terminal palettes never take over DESIGN.md app chrome.
- **deferred-needs-new-api (deferred, documented):** after the G012 burn-down, remaining deferrals are `/btw`, goal mutation, `/memory`, `/contribute-pr`, plugin marketplace install/uninstall, failed-job/monitor/cron mutations, and HTML export. Session move, provider add, OAuth login, skill/extension toggles, plugin settings mutations, and per-role model assignment are no longer deferred.
- **excluded-terminal-only (excluded, user-confirmed):** /background, /debug, /ssh (raw terminal), /exit, raw terminal appearance detection (OSC), raw extension terminal widgets, terminal process-control exec flows.

### Release-gate evidence (this audit run)
- `cargo test -p gjc-app-server`: pass (incl. strict unknown-field + notification-envelope + method-catalog + masking tests). `cargo test -p gjc-desktop`: 8 pass. `cargo clippy -p gjc-app-server -p gjc-desktop --all-targets`: 0 warnings. `cargo build -p gjc-desktop`: compiles.
- `bun --cwd=packages/gjc-app-server-client run check` (tsc + type-drift + method-catalog drift): pass; client tests: pass.
- `bun run check:schemas` (schema determinism + client generate --check): pass.
- `bun --cwd=packages/gjc-gui run check` + `run build`: pass; GUI logic tests (transcript/palette/session/model/extensibility/scroll-follow): 39 pass.
- **Diff audit:** no legacy transport (rpc/rpc-ui/bridge/bridge-client/python-gjc-rpc), harness-adapter, benchmark, or ACP files modified. All changes are within `crates/gjc-app-server`, `crates/gjc-desktop`, `packages/gjc-app-server-client`, `packages/gjc-gui`, the app-server host seam (`agent-session-host.ts`), and `schemas/app-server.schema.json`.
- **Generated-client SSOT:** request map + wrappers + notification envelope generated/drift-checked from the Rust-owned method catalog (G002); GUI uses generated types only (no hand-duplicated protocol DTOs).
- **DESIGN.md conformance:** every GUI surface reviewed by the architect against DESIGN.md (warm-dark, monospace, hairline borders, tight radii, red-claw narrow accent, :focus-visible outlines, no floaty shadows); visual QA artifacts per goal in `artifacts/g003..g007-*`.
- **Packaged macOS smoke:** the sidecar-embedding pipeline is unchanged by this run (diff touches no embed/loader code); it was proven end-to-end in the prior run's G011 (native-embedding self-heal + full chat turn without preinstalled gjc). `crates/gjc-desktop` contract tests (8) + compile confirm the shell/sidecar contract remains green. A fresh full packaged-app launch smoke against a live model is the one residual best run in a packaging environment.

### G008 audit revision (post final-review)
- Per-row G008-status finalized (no `pending` rows): implemented rows include appearance/theme/symbolPreset/colorBlindMode through strict appearance DTOs plus GUI-local preview/restore; remaining deferred rows are contract/provenance gaps only.
- `/copy` + `/dump` (in-scope-existing) NOW IMPLEMENTED as GUI-side transcript serialization/clipboard (transcript-export-logic.ts + main.tsx actions + 4 unit tests) — resolves the architect blocker.
- `/retry` and "retry last turn" IMPLEMENTED through native strict `gjc/retry` backed by `AgentSession.retry()`; `/btw` remains deferred pending an ephemeral side-turn API equivalent to TUI `runEphemeralTurn` (no tools, no persistence, current-session context); `/goal` read is `gjc/goal/read`, mutation remains deferred.
- `/help` + `/hotkeys` are prompt-display-only: surfaced in the command palette and inserted as prompts (their display behavior); no backend mutation.
- **Packaged macOS live smoke — EXPLICITLY WAIVED for this run** (approved-by: leader/ultragoal). Rationale: this run modified no embed/loader/packaging code (diff audit); the sidecar-embedding pipeline was proven end-to-end in the prior run's G011 (native-embedding self-heal + full chat turn without preinstalled gjc); `crates/gjc-desktop` compiles and its 8 contract tests pass. A fresh full `tauri build` + live-model launch is deferred to a packaging environment and recorded as the sole residual; it is not a code-correctness gap.

## G010 closeout — release-gate rerun (forked session 019f3670)
- **Historical G010 counts:** superseded by the G012 tally above. The post-burn-down matrix now reconciles to 27 in-scope-existing + 58 in-scope-new + 6 deferred-needs-new-api + 2 prompt-display-only + 2 in-scope-gui-local + 1 mixed goal read/mutation row + 7 excluded-terminal-only = **103**.
- **Release gates rerun GREEN:** `cargo test -p gjc-app-server` (93 unit + 41 conformance); `cargo test -p gjc-desktop` (8 contract tests — sidecar contract, token redaction, discovery validation); `cargo clippy -p gjc-app-server -p gjc-desktop --all-targets` (finished, no errors; residual clippy suggestions are style-only — doc backticks/paragraph length, raw-string hashes, format-arg inlining — in lib doc-comments + the conformance test, not correctness); `cargo build -p gjc-desktop` (compiles); `bun run check:schemas` (up to date); `bun --cwd=packages/gjc-app-server-client run check + test` (7 pass); `bun --cwd=packages/gjc-gui run check + build (269 kB) + test` (68 pass).
- **Banned-path audit PASS:** no `gjc/commands/execute` anywhere; `command/exec` and `thread/shellCommand` are pre-existing lenient codex-core methods (schema `strict=false`), not introduced by this run and not routed to by any parity feature; every new parity/backend-affecting method is native strict `gjc/*` (retry, goal/read, context/read, session list/search/tree/rename/export, exec-state reads, model/thinking/fast/settings, appearance, provider/auth, read catalogs). No TUI-handler replay or GUI raw-request-string path.
- **HANDOFF residuals:** startup-hang CLOSED (G001, native addon build; app-server emits ready+initialize < 1s). Packaged macOS launch smoke + real sidecar embedding remain the **sole waived residual** (requires a packaging environment); this session used a transparent dev sidecar stub at `crates/gjc-desktop/binaries/gjc-aarch64-apple-darwin` solely to run the 8 desktop contract tests and made NO embed/loader/packaging code changes.

## G012 closeout — GUI⇄TUI parity burn-down (masterplan execution)

- **Masterplan execution:** `docs/gui-tui-parity-masterplan.md` was executed across six ultragoal stories: G001 session/history closure, G002 live streaming and exec-state push closure, G003 catalog/palette/extensibility closure, G004 stale-deferral API closures, G005 GUI beautiful-embedding presentation closure, and G006 matrix reconciliation. This section reconciles the matrix to the post-closure shipped state.
- **New strict `gjc/*` surface represented in this matrix:** `gjc/session/open`, `gjc/session/delete`, `gjc/session/navigate`, `gjc/session/label`, `gjc/session/move`, `gjc/provider/add`, `gjc/auth/login/start`, `gjc/auth/login/poll`, `gjc/auth/login/complete`, `gjc/auth/login/cancel`, `gjc/model/assign`, `gjc/extensions/setEnabled`, `gjc/skills/setEnabled`, `gjc/plugins/setEnabled`, `gjc/plugins/setFeature`, `gjc/plugins/setSetting`, plus `cwd?` on `gjc/session/list` and `gjc/session/search`, `gjc/jobs/changed`, `GjcCronEntry`/`outputTail` monitor read-model fields, richer `ExtensionDescriptor` fields, and `gjcWorkflowGateList` reconnect resync.
- **Release gates:** shared run evidence is green: `cargo test -p gjc-app-server` (97 unit + 48 conformance), app-server clippy clean, `cargo test -p gjc-desktop` (8 pass), `bun run check:schemas` up to date, client check + 7 tests, GUI check + build (299KB) + 114 tests, and coding-agent app-server suites 60 pass.
- **QA artifact references:** per-story quality gates are `artifacts/parity/qa/g001-quality-gate.json`, `artifacts/parity/qa/g002-quality-gate.json`, `artifacts/parity/qa/g003-quality-gate.json`, `artifacts/parity/qa/g004-quality-gate.json`, and `artifacts/parity/qa/g005-quality-gate.json`, with associated red-team receipts and GUI browser screenshot evidence for the shipped surfaces.
- **Implementation invariant:** every implemented backend-affecting row added by this burn-down is native Rust, strict-field, schema-registered `gjc/*`, has a generated client wrapper, strict unknown-field coverage, GUI tests, and live browser screenshot evidence. Approved existing `thread/*`/`gjc/*` rows remain schema-typed as noted in their rows.
- **Residual deferrals:** `/btw` still needs a `runEphemeralTurn` seam; goal mutation still needs a confirmation-policy mutation seam; `/memory` has a store owner but still needs a redaction/provenance-safe read/clear/enqueue contract; `/contribute-pr` needs SCM credential/provenance policy; plugin marketplace install/uninstall still needs supply-chain review; failed-job ack/monitor cancel/cron delete need strict confirmation-gated mutation DTOs; HTML export is explicitly deferred until `html` is added to the `gjc/session/export` format enum.
- **Banned paths remain banned:** no row is implemented through `gjc/commands/execute`, `command/exec`, `thread/shellCommand`, TUI-handler replay, GUI raw request strings, or `gjc/notifications/*` outside the reserved notifications SDK exception.
